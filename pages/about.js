import Layout from '../components/Layout';

export default function About() {
    return (
        <Layout>
            <div>
                <p>This is the about page</p>
            </div>
        </Layout>
    );
};